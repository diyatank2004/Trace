version: "3.9"

services:
  backend:
    build:
      context: ./trace-backend
      dockerfile: Dockerfile
    container_name: trace-backend
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://tracker:project_tracker@database-1.crakskeyahiw.ap-southeast-2.rds.amazonaws.com:5432/trace_db
      SECRET_KEY: thisisthesecretkeyforjwtsigning
      ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: 720
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./trace-frontend
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: http://3.104.49.129:8000
    container_name: trace-frontend
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "3000:80"
